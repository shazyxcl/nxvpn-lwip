<?xml version="1.0" encoding="utf-8"?>
<!--
    profile_item_menu.xml
    Popup menu shown when the user taps the ⋮ button on a profile row.
    IDs must match those checked in ProfileAdapter's PopupMenu click handler.
-->
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/menu_edit_profile"
        android:title="@string/edit_profile"/>

    <item
        android:id="@+id/menu_delete_profile"
        android:title="@string/delete_profile" />

    <item
        android:id="@+id/menu_export_profile"
        android:title="@string/export_profile"/>

</menu>
